meta {
  name: Get CSRF token
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/auth/csrf
  body: none
  auth: none
}

script:post-response {
  bru.setEnvVar("csrfToken", res.body.token);
}

assert {
  res.status: eq 200
}
